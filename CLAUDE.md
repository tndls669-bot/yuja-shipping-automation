# 순수유자 배송자동화 — 운영 규칙서

이 파일은 이 저장소에서 작업하는 모든 Claude 세션(어떤 모델이든)이 **가장 먼저** 읽는 문서다.
2026-08-30~09-02 서버 이전 기간에 실제 사고를 겪으며 정리한 규칙이라, 여기 적힌 것은
추측이 아니라 전부 실제로 일어난 일에서 나왔다. 이 규칙을 지키면 Sonnet급 모델로도
일상 운영이 안전하고, 어기면 어떤 모델이든 같은 사고를 낸다.

## 0. 절대 규칙 (먼저 읽을 것)

1. **2단계(우체국 접수)는 실제 배송비가 나가고 되돌릴 수 없다.** 대표님이 "접수해"라고
   명시적으로 말한 뒤에만 실행한다. 실행 전 §4 체크리스트를 반드시 통과시킨다.
2. **CSV를 손으로 고칠 때 쉼표가 들어간 값은 반드시 큰따옴표로 감싼다.**
   `"경기도 안산시 (사동, 선경아파트)"` 처럼. 안 감싸면 뒤 칸이 밀려 우편번호가 중량 칸에
   들어가고, 그 결과 15,621kg 주문 1건이 8kg박스 1,954개로 쪼개져 전부 실제 접수된 적이
   있다(2026-09-01). 코드에 3중 방어(§6)가 있지만 방어에 기대지 말고 애초에 지킬 것.
3. **`data/inbox/` 아래 파일은 고객 개인정보다.** 절대 git에 올리지 않는다(.gitignore에
   있음). 대화에 내용을 옮길 때도 필요한 최소한만. 이 폴더의 파일을 지우거나 덮어쓰는
   작업은 안전장치에 막힐 수 있으니, 막히면 우회하지 말고 대표님께 말한다.
4. **비밀번호·API 키는 `.env`에만 있다.** 어떤 파일·커밋·대화에도 값을 옮기지 않는다.
   웹 로그인(우체국 계약고객시스템, 네이버 판매자센터, 공유기 등)은 Claude가 대신 하지
   않는다 — 대표님이 직접 한다.
5. **모르면 추측하지 말고 확인한다.** 우편번호는 정부 주소검색으로, 네이버 주문 상태는
   API 조회로, 메일이 몇 건 왔는지는 실제 메일함으로. "아마 그럴 것"으로 데이터를 만들지
   않는다.
6. **커밋·푸시는 대표님이 시킬 때만.** 커밋 전 `git status`로 `.env`, `data/inbox/`,
   `vendor/`가 안 들어갔는지 확인한다. 작성자는 이 저장소에 설정된 값(Suin Chae)을 쓴다.

## 1. 이 시스템이 하는 일

전남 고흥 유자 농장(순수유자)의 주문을 여러 채널에서 모아 표준 형태로 만들고, 우체국
계약소포 API로 접수해 운송장번호를 받고, 채널별로 발송 처리를 돌려주는 자동화다.

주문 채널(`주문처`):
- **네이버스마트스토어** — 커머스 API로 자동 조회 (`naver_order_api_import.py`)
- **위탁판매자** — 등록된 거래처 이메일을 읽어 파싱. 현재 팔도맘, 남해로부터, 어글리어스.
  거래처 목록과 표시 이름은 `data/wholesale_senders.txt` (형식 `이메일,주문처이름`)
- **전화문자** — 대표님이 본인 메일로 정리해 보낸 주문. `data/phone_text_senders.txt`

이메일 본문/엑셀 첨부는 Gemini(`gemini-3.7-flash`, 버전 고정)로 파싱하고, 어글리어스는
표 형식이 정해져 있어 `uglyus_order_import.py`가 직접 파싱한다.

## 2. 매일 운영 절차

1. **1단계 실행** — 바탕화면 `1_오늘주문처리` 또는 아래 명령.
   모든 채널에서 오늘 주문을 모아 `data/inbox/{날짜}/output/{날짜}_orders.csv`에 쓰고,
   오더 리포트 메일을 보낸다. 하루에 여러 번 돌려도 된다(이미 CSV에 있는 주문번호는
   다시 쓰지 않음).
2. **리포트/CSV 검토** — 정상 N건, 확인필요 M건, "처리 실패한 소스"가 있는지 본다.
   - 확인필요(`{날짜}_issues.csv`)는 우편번호 누락·전화번호 이상 등. 고쳐서 `_orders.csv`로
     옮긴다(§3).
   - `재처리필요_*.txt`가 생겼으면 Gemini 호출이 실패한 메일이다(§5).
   - 대표님이 합포장·제외 등을 원하면 `_orders.csv`를 엑셀로 고친다(§3).
3. **2단계 실행** — 대표님 확인 후 `2_우체국접수확정`. 실제 접수, 운송장번호 발급,
   네이버 발송처리 API 자동 등록, `{날짜}_epost_result.csv` 기록, `{날짜}_print.html`(A4
   포장 시트) 생성, 결과 메일 발송까지 한 번에.
4. **거래처 송장 안내** — 위탁판매자 주문이 있었으면 그 거래처 메일에 **답장**으로
   `받는사람: 운송장번호` 목록을 보낸다(§7). 자동화되어 있지 않으므로 대표님이 "송장
   뿌려줘"라고 하면 Claude가 Gmail 커넥터로 보낸다. 스마트스토어는 2단계에서 API로
   자동 처리되므로 따로 할 일 없다.
5. **인쇄** — `{날짜}_print.html`을 브라우저로 열어 Ctrl+P. 위쪽 종류별 개수 요약으로
   박스 준비, 아래 목록으로 운송장 대조.

### 직접 실행 명령 (배치파일 대신 PowerShell에서)
```powershell
$env:PYTHONPATH = "C:\yuja-shipping-automation\vendor"; $env:PYTHONIOENCODING = "utf-8"
cd C:\yuja-shipping-automation
& "C:\Program Files\Python312\python.exe" src\local_step1_process_orders.py   # 1단계
& "C:\Program Files\Python312\python.exe" src\local_step2_epost_submit.py     # 2단계 (실접수!)
```
`vendor/`는 이 PC 전용 패키지 폴더(pip --target). 새 PC면
`pip install --target vendor openpyxl msoffcrypto-tool bcrypt pytest`.

## 3. `{날짜}_orders.csv` — 유일한 진실의 원천

2단계는 **이 CSV를 그대로 읽어 접수**한다. 대표님이 엑셀로 고친 내용이 그대로 반영된다.

컬럼 순서(14개, 바꾸지 말 것):
`채널구분, 주문처, 원주문번호, 품목군, 세부품목, 받는사람, 전화번호, 주소, 상세주소, 우편번호, 중량또는수량, 박스구성, 배송메시지, 지정일여부`

- **주문 합치기(합포장)**: 한 행의 `중량또는수량`을 합산값으로 고치고 나머지 행을 지운다.
  `박스구성`은 안 고쳐도 된다(읽을 때 중량으로 다시 계산). 단, **지운 행의 네이버 주문은
  발송처리가 자동으로 안 되므로** 접수 후 판매자센터에서 같은 운송장번호를 수동 입력해야
  한다(§5). 또 지운 주문번호가 `data/naver_api_state.json`의 `pending_order_ids`에 남아
  있으면 다음 1단계에서 다시 나타나므로 거기서도 빼준다.
- **주문 빼기(보류)**: 행을 지운다. 네이버 주문은 상태가 PAYED인 한 다음 실행 때 다시
  나타난다(정상 — 나중에 보낼 때 그대로 두면 됨).
- **우편번호**: 없으면 우체국 API가 접수를 거부한다. 남해로부터는 요청 후 2026-09-03부터
  주문서 양식에 우편번호 칸을 추가해서 보내주고 있다(팔도맘·어글리어스는 원래부터 있음).
  그래도 빠져 있으면 juso.go.kr에서 찾아 넣는다 — Claude가 "기억"으로 채우지 않는다.
- **중량**: 청유자/유자는 kg(0.5, 1, 1.5, 3…), 유자청은 병 수. **50 초과면 데이터 오류**로
  자동 차단된다.
- **8kg박스 실제 한계는 품목마다 다르다**: 유자(노란유자)는 8kg가 한계라 그 이상이면
  다음 박스로 나뉜다. **청유자(녹색)는 밀도가 높아 8kg박스 하나에 10kg까지 실제로
  들어간다**(대표님 확인, 2026-09-03) — `box_calc.CHEONGYUJA_8KG_BOX_CAPACITY`에 반영돼
  있어 청유자 10kg 주문은 자동으로 1박스로 계산된다. 이 숫자가 바뀌면(예: 11kg까지 확인됨)
  그 상수만 고치면 된다.

## 4. 2단계 실행 전 체크리스트

- [ ] 대표님이 접수를 명시적으로 지시했다.
- [ ] `_issues.csv`가 비어 있거나, 남은 건은 오늘 안 보내기로 한 것이다.
- [ ] 1단계 리포트의 종류별 요약 건수/중량이 상식적이다(예: 청유자 총 20kg, 30건). 이상하게
      크면 CSV 칸 밀림을 의심한다.
- [ ] 이미 접수된 주문은 `_epost_result.csv`의 "접수완료" 기록으로 자동 스킵되므로,
      재실행이 중복 접수를 만들지 않는다. 다만 이 파일을 임의로 지우면 그 보호가 사라진다.
- [ ] 실행 후 결과의 총 건수가 CSV 행 수(±박스 분할)와 맞는지 확인한다. 몇 배로 부풀어
      있으면 즉시 §5 "중복 접수" 절차.

## 5. 예외 상황별 대응

| 상황 | 대응 |
|---|---|
| 리포트에 "처리 실패한 소스", `재처리필요_*.txt` 생김 | Gemini 호출 실패(주로 타임아웃). 그 메일은 이미 "처리됨"으로 표시돼 다시 안 읽힌다. 파일 내용을 `parse_orders_from_text`로 다시 파싱하거나(재시도 로직이 3회까지 자동으로 붙어 있음) 내용을 보고 CSV에 직접 추가한다 — 이때 §3 규칙 준수. |
| 위탁 메일이 왔는데 리포트에 없음 | `data/processed_email_ids.json`에 이미 기록됐는지 확인. 기록됐는데 주문이 없으면 위 케이스다. |
| 네이버 주문이 안 들어옴 | 네이버 `last-changed-statuses` API는 하루 정도의 짧은 창만 준다. 며칠 안 돌렸으면 12시간 단위로 나눠 조회해 `pending_order_ids`를 채운다(`naver_order_api_import.py` 주석 참고). |
| 합포장으로 지운 네이버 주문이 "미발송"으로 남음 | 정상. 판매자센터에서 그 주문에 같은 운송장번호를 수동 입력. |
| 접수 결과가 수십~수천 배로 부풀어 있음 (중복 접수) | 즉시 대표님께 알린다 → 대표님이 우체국 우편고객센터(1588-1300, 평일 09~18시, 상담원은 2번→0번)에 계약고객번호(.env의 `EPOST_CUST_NO`)와 상황을 말해 취소 요청 → 취소 확정 후 `_epost_result.csv`에서 가짜 행을 지우되 **실제로 붙인 운송장번호 1줄은 남긴다** → `print.html` 재생성. 취소된 등록은 재접수하지 않는다(물건은 이미 나갔으므로). |
| 우체국 접수 1건 취소 필요 | 코드에 취소 API 없음. 대표님이 계약고객시스템/고객센터로 취소. |
| 어글리어스 주문 | 우체국 접수 후 `{날짜}_어글리어스송장등록.xlsx`가 생기면 어글리어스 사이트에 업로드(대표님). |
| 네이버 발송처리 API 실패 | `{날짜}_네이버발송처리.xlsx`가 생기며 판매자센터 엑셀 일괄발송으로 대체(대표님). |

## 6. 코드에 들어있는 안전장치 (있는지 알고, 없애지 말 것)

- `box_calc.calc_boxes`: 50kg 초과 시 `ValueError` — 수천 개 박스로 쪼개지는 것 차단.
  `eight_kg_box_capacity` 인자로 품목별 8kg박스 실제 한계를 다르게 줄 수 있다(§3 참고).
- `validation.validate_order`: 우편번호 누락, 전화번호 형식, 중량 50 초과를 "확인필요"로.
- `local_step2_epost_submit.run`: 접수 직전에 `validate_order`를 다시 돌려 이상 건은 API
  호출 자체를 안 한다. 이미 접수완료된 주문번호는 스킵.
- `csv_export.read_orders_csv`: 손상된 행 하나 때문에 파일 전체를 못 읽지 않는다.
- `local_step1`: 오늘 CSV에 이미 있는 주문번호는 다시 쓰지 않는다. Gemini 실패 시 원문을
  `재처리필요_*.txt`로 남기고, 그 저장이 실패해도 실패 사실은 리포트에 남긴다.
- `gemini_client`: 60초 타임아웃, 3회 재시도, 모델 버전 고정.

## 7. 거래처 송장 안내 메일 양식

거래처가 보낸 주문서 메일에 **답장**으로 보낸다. 인사말은 반드시 **"안녕하세요, 순수유자입니다."**
(상대 회사 이름을 인사말에 쓰는 실수를 한 적 있음).
```
안녕하세요, 순수유자입니다.

보내주신 주문 우체국 택배 접수 완료했습니다. 운송장번호 안내드립니다.

- 받는사람A: 운송장번호
- 받는사람B: 운송장번호

감사합니다.
```
운송장번호는 `{날짜}_epost_result.csv`에서 `주문처`(= `_orders.csv`의 주문처)별로 뽑는다.

## 8. 파일·데이터 지도

```
data/inbox/{날짜}/output/
  {날짜}_orders.csv       접수 대상(진실의 원천). 사람이 고쳐도 됨. PII.
  {날짜}_issues.csv       확인필요 건. 고쳐서 orders로 옮김.
  {날짜}_epost_result.csv 접수 기록 + 중복접수 방지 대장. 함부로 지우지 말 것.
  {날짜}_print.html       A4 포장 시트(접수 후 생성, 그날 전체 기준).
  재처리필요_*.txt         Gemini 파싱 실패 원문.
data/naver_api_state.json    네이버 미발송 주문 추적. git 커밋 대상.
data/aggregate_log.csv       PII 없는 집계 로그(누적 통계 원천). git 커밋 대상.
data/cumulative_data.json, data/output/dashboard.html  집계에서 매번 재계산됨.
data/wholesale_senders.txt, data/phone_text_senders.txt  발신자 목록. "이메일,주문처이름".
data/processed_email_ids.json  이미 읽은 메일 ID.
```
`.env`(비밀), `data/inbox/`(PII), `vendor/`(패키지)는 git에 올라가지 않는다.

## 9. 코드 변경 시

```powershell
$env:PYTHONPATH = "C:\yuja-shipping-automation\vendor;C:\yuja-shipping-automation\src"
cd C:\yuja-shipping-automation; & "C:\Program Files\Python312\python.exe" -m pytest tests/ -q
```
바꾼 뒤 항상 테스트를 돌린다. 사고에서 배운 규칙을 코드로 옮길 때는 그 사고를 주석에
날짜와 함께 남긴다(이미 그렇게 되어 있음). 파이프라인은 `local_step1` → `local_step2`
하나뿐이다(옛 `daily_pipeline`은 2026-09-02에 제거 — 되살리지 말 것).

## 10. 인프라

- 이 코드는 헤드리스 미니 PC(Windows 11 Pro, `DESKTOP-TLDI3IS`)에서 돈다. 모니터 없음.
- 접속: 같은 와이파이에서는 원격 데스크톱(고정 IP), 밖에서는 Tailscale 앱으로 이 PC 이름
  선택 후 원격 데스크톱. 계정 `USER`. 원격 창을 닫는 건 "연결 끊기"이고 세션은 살아 있다.
  시작 메뉴의 "로그아웃"만 누르지 않으면 된다.
- 절전 모드 "안 함" 설정됨. 재부팅 후에도 원격 접속 정상 확인됨(2026-08-31).
- 파이썬은 `C:\Program Files\Python312\python.exe`. 이 세션(Claude 앱)에서 pip로 설치한
  패키지는 샌드박스 때문에 배치파일에서 안 보일 수 있다 — 반드시 `--target vendor`로.

## 11. 보류/미구현 (이미 결정된 것 — 다시 제안하지 말 것)

- 우편번호 자동조회 API: **안 한다.** 거래처 주문서에 우편번호 칸을 넣기로 함.
- 쿠팡 연동: Wing API 키 발급 후 진행. 아직 없음.
- 우체국 라벨(ZD230) 자동 인쇄: 계약소포 API가 라벨 데이터를 주지 않고 웹+오즈뷰어로만
  가능. 포스트플러스 전환 여부 미결정. 보류.
- 위탁판매자 송장 자동 메일: 미구현. 대표님 요청 시 Claude가 수동 발송(§7).
- Gemini 크레딧(지원사업 30만원/년) 소진용 후보: "검토자 2차 호출"(파싱 결과를 원문과
  대조 검증). 대표님이 원할 때 구현.

## 12. 모델 운용

- 일상 운영(§2 절차, CSV 검토, 송장 메일)은 **Sonnet**으로 충분하다 — 이 문서를 따르면 된다.
- 다음 경우는 상위 모델(Opus/Fable)로 올린다: 주문이 사라졌거나 건수가 안 맞을 때, 접수
  결과가 이상할 때, 코드 구조를 바꿀 때, 새 채널/API를 붙일 때.
- 어떤 모델이든 §0 절대 규칙은 같다.
